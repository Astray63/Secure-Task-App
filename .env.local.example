# Ce fichier surcharge .env pour le développement local (non-commité)
# Copiez en .env.local puis adaptez

# API
NODE_ENV=development
PORT=3000

# Base de données (exécutez `docker compose up -d db` et utilisez le port 3307)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=task_user
DB_PASSWORD=change_me
DB_NAME=task_app
DB_CONNECTION_LIMIT=10

# Sécurité / Auth
JWT_SECRET=change_this_secret_key_please
JWT_EXPIRES_IN=1h
BCRYPT_SALT_ROUNDS=12

# Logs
LOG_LEVEL=info
